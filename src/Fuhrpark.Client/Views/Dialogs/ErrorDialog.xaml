<Window x:Class="Fuhrpark.Client.Views.Dialogs.ErrorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Fehler" Height="330" Width="620"
        WindowStartupLocation="CenterOwner" ResizeMode="CanResize"
        Background="{StaticResource SurfaceBrush}" ShowInTaskbar="False">
  <Grid Margin="18">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <TextBlock x:Name="TitleText" Style="{StaticResource PageTitleStyle}"
               Foreground="{StaticResource CriticalBrush}" Text="Fehler" />

    <TextBlock x:Name="MessageText" Grid.Row="1" Margin="0,8,0,0" TextWrapping="Wrap" />

    <Border x:Name="DetailsPanel" Grid.Row="2" Margin="0,12,0,0" Visibility="Collapsed"
            Style="{StaticResource CardStyle}" Background="{StaticResource SurfaceSunkenBrush}">
      <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
        <TextBox x:Name="DetailsText" IsReadOnly="True" BorderThickness="0"
                 Background="Transparent" FontFamily="Consolas" FontSize="11"
                 TextWrapping="NoWrap" VerticalScrollBarVisibility="Disabled" />
      </ScrollViewer>
    </Border>

    <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
      <Button Content="_Details" Click="OnToggleDetails" Margin="0,0,8,0" />
      <Button Content="Fehlerbericht _kopieren" Click="OnCopyReport" Margin="0,0,8,0" MinWidth="160" />
      <Button Content="_Schließen" Style="{StaticResource PrimaryButtonStyle}" IsDefault="True" IsCancel="True"
              Click="OnClose" />
    </StackPanel>
  </Grid>
</Window>
