<Application x:Class="Fuhrpark.ServerSetup.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Fuhrpark.ServerSetup"
             ShutdownMode="OnMainWindowClose">
  <Application.Resources>
    <local:StepVisibilityConverter x:Key="StepVisibility" />
    <local:ActiveStepConverter x:Key="ActiveStep" />
    <local:BooleanToVisibilityConverter x:Key="BoolVisibility" />
    <local:TextToVisibilityConverter x:Key="TextVisibility" />

    <SolidColorBrush x:Key="SurfaceBrush" Color="#FFFFFFFF" />
    <SolidColorBrush x:Key="SidebarBrush" Color="#FF1F2A37" />
    <SolidColorBrush x:Key="SidebarActiveBrush" Color="#FF2C3A4B" />
    <SolidColorBrush x:Key="AccentBrush" Color="#FF0D6EFD" />
    <SolidColorBrush x:Key="TextBrush" Color="#FF1C2430" />
    <SolidColorBrush x:Key="MutedBrush" Color="#FF5E6B79" />
    <SolidColorBrush x:Key="OkBrush" Color="#FF2E7D32" />
    <SolidColorBrush x:Key="CriticalBrush" Color="#FFC62828" />
    <SolidColorBrush x:Key="BorderBrush" Color="#FFD3D9DE" />
    <SolidColorBrush x:Key="PanelBrush" Color="#FFF4F6F8" />

    <Style TargetType="TextBlock">
      <Setter Property="FontFamily" Value="Segoe UI" />
      <Setter Property="FontSize" Value="13" />
      <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
    </Style>

    <Style x:Key="HeadlineText" TargetType="TextBlock">
      <Setter Property="FontFamily" Value="Segoe UI" />
      <Setter Property="FontSize" Value="19" />
      <Setter Property="FontWeight" Value="SemiBold" />
      <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
    </Style>

    <Style x:Key="CaptionText" TargetType="TextBlock">
      <Setter Property="FontFamily" Value="Segoe UI" />
      <Setter Property="FontSize" Value="12" />
      <Setter Property="Foreground" Value="{StaticResource MutedBrush}" />
      <Setter Property="Margin" Value="0,12,0,3" />
    </Style>

    <Style TargetType="Button">
      <Setter Property="FontFamily" Value="Segoe UI" />
      <Setter Property="FontSize" Value="13" />
      <Setter Property="Padding" Value="16,7" />
      <Setter Property="MinWidth" Value="120" />
      <Setter Property="Cursor" Value="Hand" />
    </Style>

    <Style TargetType="TextBox">
      <Setter Property="FontFamily" Value="Segoe UI" />
      <Setter Property="FontSize" Value="13" />
      <Setter Property="Padding" Value="6,4" />
      <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
    </Style>

    <Style TargetType="PasswordBox">
      <Setter Property="FontFamily" Value="Segoe UI" />
      <Setter Property="FontSize" Value="13" />
      <Setter Property="Padding" Value="6,4" />
      <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
    </Style>

    <Style TargetType="CheckBox">
      <Setter Property="FontFamily" Value="Segoe UI" />
      <Setter Property="FontSize" Value="13" />
    </Style>

    <Style TargetType="RadioButton">
      <Setter Property="FontFamily" Value="Segoe UI" />
      <Setter Property="FontSize" Value="13" />
    </Style>
  </Application.Resources>
</Application>
