<Window x:Class="Vehistra.Client.Views.Dialogs.PromptDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Eingabe" Height="220" Width="480" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{StaticResource SurfaceBrush}" ShowInTaskbar="False">
  <StackPanel Margin="18">
    <TextBlock x:Name="MessageText" TextWrapping="Wrap" Margin="0,0,0,10" />
    <TextBox x:Name="InputBox" AcceptsReturn="True" MinHeight="60" TextWrapping="Wrap"
             VerticalScrollBarVisibility="Auto" />
    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
      <Button Content="Abbre_chen" IsCancel="True" Margin="0,0,8,0" />
      <Button Content="_OK" Style="{StaticResource PrimaryButtonStyle}" IsDefault="True" Click="OnOk" />
    </StackPanel>
  </StackPanel>
</Window>
