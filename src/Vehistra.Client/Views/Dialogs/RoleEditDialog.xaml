<UserControl x:Class="Vehistra.Client.Views.Dialogs.RoleEditDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="820">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <Border Style="{StaticResource ToolbarStyle}" Padding="18,12">
      <StackPanel>
        <TextBlock Text="{Binding Title}" Style="{StaticResource PageTitleStyle}" />
        <TextBlock Style="{StaticResource PageSubtitleStyle}" TextWrapping="Wrap"
                   Text="Die Berechtigungen werden in allen Fachdiensten geprüft – nicht nur beim Ausblenden von Schaltflächen." />
      </StackPanel>
    </Border>

    <StackPanel Grid.Row="1" Margin="18,18,18,10">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="12" />
          <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>
        <StackPanel>
          <TextBlock Text="Schlüssel *" Style="{StaticResource FieldLabelStyle}" />
          <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsNew}"
                   MaxLength="64" />
        </StackPanel>
        <StackPanel Grid.Column="2">
          <TextBlock Text="Anzeigename *" Style="{StaticResource FieldLabelStyle}" />
          <TextBox Text="{Binding DisplayName, UpdateSourceTrigger=PropertyChanged}" MaxLength="128" />
        </StackPanel>
      </Grid>

      <TextBlock Text="Beschreibung" Style="{StaticResource FieldLabelStyle}" Margin="0,12,0,2" />
      <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" />
    </StackPanel>

    <ScrollViewer Grid.Row="2" MaxHeight="420" VerticalScrollBarVisibility="Auto" Padding="18,0,18,10">
      <ItemsControl ItemsSource="{Binding PermissionGroups}">
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <Border Style="{StaticResource CardStyle}" Margin="0,0,0,10">
              <StackPanel>
                <Grid Margin="0,0,0,6">
                  <TextBlock Text="{Binding Group}" FontWeight="SemiBold" VerticalAlignment="Center" />
                  <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Alle" MinWidth="60" Command="{Binding SelectAllCommand}" />
                    <Button Content="Keine" Margin="6,0,0,0" MinWidth="60" Command="{Binding SelectNoneCommand}" />
                  </StackPanel>
                </Grid>
                <ItemsControl ItemsSource="{Binding Permissions}">
                  <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                      <UniformGrid Columns="3" />
                    </ItemsPanelTemplate>
                  </ItemsControl.ItemsPanel>
                  <ItemsControl.ItemTemplate>
                    <DataTemplate>
                      <CheckBox Content="{Binding DisplayName}" IsChecked="{Binding IsSelected}"
                                ToolTip="{Binding Name}" Margin="0,2,12,2" />
                    </DataTemplate>
                  </ItemsControl.ItemTemplate>
                </ItemsControl>
              </StackPanel>
            </Border>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </ScrollViewer>

    <Border Grid.Row="3" Background="{StaticResource CriticalBackgroundBrush}" Padding="18,8"
            Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibility}}">
      <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource CriticalBrush}" TextWrapping="Wrap" />
    </Border>

    <Border Grid.Row="4" Background="{StaticResource SurfaceAltBrush}" BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="0,1,0,0" Padding="18,12">
      <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
        <Button Content="_Speichern" Style="{StaticResource PrimaryButtonStyle}" MinWidth="140" IsDefault="True"
                Command="{Binding ConfirmCommand}" />
        <Button Content="Abbre_chen" Margin="8,0,0,0" IsCancel="True" Command="{Binding CancelCommand}" />
      </StackPanel>
    </Border>
  </Grid>
</UserControl>
